@startuml
!include ./include/C4_Component.puml

title C4 Level 3 – Components (Master AutoSetup)

Container(master, "Master AutoSetup", "Avalonia .NET") {

  Component(ui, "UI Layer", "XAML + Views")
  Component(vm, "ViewModels", "MVVM Binding")
  Component(engine, "WorkflowEngine", "Оркестратор шагов")
  Component(controls, "Controls", "Модули проверки/фикса")
  Component(logging, "Logging & Reports", "Локальные журналы и отчёты")
}

Rel(ui, vm, "MVVM Binding")
Rel(vm, engine, "Запуск процессов")
Rel(engine, controls, "Check() / Fix()")
Rel(controls, logging, "Логирует")

@enduml